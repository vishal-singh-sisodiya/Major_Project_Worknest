# Local dev: leave empty — Vite proxies /api and /socket.io to localhost:5000 (see vite.config.js)

# Production: your deployed API origin only — e.g. https://worknest-api.onrender.com
# (no trailing slash; no /api — axios adds /api automatically)
VITE_API_URL=
